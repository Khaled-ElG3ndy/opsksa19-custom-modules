<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_gr_contract_amendment_form" model="ir.ui.view">
        <field name="name">gr.contract.amendment.form</field>
        <field name="model">gr.contract.amendment</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_submit" type="object" string="Submit"
                            class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_approve" type="object" string="Approve"
                            class="btn-primary" invisible="state != 'submitted'"
                            groups="gr_security_base.group_generator_general_manager"/>
                    <button name="action_apply" type="object" string="Apply to Contract"
                            class="btn-primary" invisible="state != 'approved'"/>
                    <button name="action_reject" type="object" string="Reject"
                            invisible="state != 'submitted'"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,submitted,approved,applied"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="contract_id"
                                   readonly="state != 'draft'"/>
                            <field name="requested_by"/>
                            <field name="request_date"/>
                        </group>
                        <group>
                            <field name="approved_by"/>
                            <field name="approval_date"/>
                            <field name="currency_id" invisible="1"/>
                        </group>
                    </group>
                    <group string="Reason">
                        <field name="reason" nolabel="1" readonly="state != 'draft'"/>
                    </group>
                    <group string="Proposed New Values (leave blank to keep current)">
                        <group>
                            <field name="new_included_hours_per_day" readonly="state != 'draft'"/>
                            <field name="new_max_hours_per_day" readonly="state != 'draft'"/>
                            <field name="new_base_daily_rate" readonly="state != 'draft'"/>
                            <field name="new_base_monthly_rate" readonly="state != 'draft'"/>
                        </group>
                        <group>
                            <field name="new_hourly_rate" readonly="state != 'draft'"/>
                            <field name="new_overtime_hour_rate" readonly="state != 'draft'"/>
                            <field name="new_violation_hour_rate" readonly="state != 'draft'"/>
                        </group>
                    </group>
                    <group string="Audit" invisible="state != 'applied'">
                        <field name="old_values_json" readonly="1"/>
                        <field name="new_values_json" readonly="1"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_gr_contract_amendment_list" model="ir.ui.view">
        <field name="name">gr.contract.amendment.list</field>
        <field name="model">gr.contract.amendment</field>
        <field name="arch" type="xml">
            <list decoration-muted="state in ('rejected','applied')">
                <field name="name"/>
                <field name="contract_id"/>
                <field name="requested_by"/>
                <field name="request_date"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'applied'"
                       decoration-info="state == 'approved'"
                       decoration-warning="state == 'submitted'"
                       decoration-danger="state == 'rejected'"/>
            </list>
        </field>
    </record>

    <record id="action_gr_contract_amendment" model="ir.actions.act_window">
        <field name="name">Contract Amendments</field>
        <field name="res_model">gr.contract.amendment</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
